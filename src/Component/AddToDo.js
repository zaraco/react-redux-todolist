import React from 'react';

const AddToDo = () => {

    return (
        <div>
            <input type='text' value/>
            <button>Add</button>
        </div>
    );
};

export default AddToDo;